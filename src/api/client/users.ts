import type { ClientUser } from './types';

export async function getUser(id: string): Promise<ClientUser | undefined> {
  return undefined;
}

export async function searchUsers(query: string): Promise<ClientUser[]> {
  return [];
}